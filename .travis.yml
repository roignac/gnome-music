sudo: required
services: docker
language: python
env:
 - TEST="pyflakes"
 - TEST="pep8"
 - TEST="build"

matrix:
  allow_failures:
    - env: TEST="pep8"

script:
- docker run -itv $(pwd):/mnt --rm fedora:rawhide "/bin/sh" -e "PR=$TRAVIS_PULL_REQUEST" "/mnt/ci_$TEST.sh"
